server:
  port: 8761
  
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: ${DISCOVERY_URL:http://localhost:8761}/eureka/

management:
  security:
    enabled: false
spring:
  boot:
    admin:
      context-path: /admin